# Optional overrides — copy to .env and uncomment to customize.
# Paths are relative to the repo root (plant-tracking/).

# DATABASE_URL=sqlite:///./data/plant_tracking.db
# UPLOAD_DIR=./data/uploads
# DATA_DIR=./data
